version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
  containers:
    - name: main
      image: semaphoreci/ruby:2.6.3

blocks:
  - name: __test
    dependencies: []
    task:
      jobs:
        - name: test
          parallelism: 10
          commands:
            - checkout
            - gem install semaphore_test_boosters
            - echo "This is job $SEMAPHORE_JOB_INDEX/$SEMAPHORE_JOB_COUNT"
            
